# Screenshot recognition weights

Place these two files in this directory before running `setup-local.ps1`:

- `board.pth`
- `stone.pth`

They are available from the upstream
[noword/image2sgf releases](https://github.com/noword/image2sgf/releases).
The large weight files are intentionally excluded from Git; confirm the
upstream license before redistributing them.
