# Human-readable inputs for requirements.lock.txt. The Windows runtime setup
# consumes the generated lock file and verifies every downloaded wheel hash.
flask==3.1.3
flask-socketio==5.6.1
simple-websocket==1.1.0
